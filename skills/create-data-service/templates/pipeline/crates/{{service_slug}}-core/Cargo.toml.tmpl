[package]
name = "{{service_slug}}-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Core pipeline traits + types for {{service_title}} (Substrate -> Handler -> Sink)."

[dependencies]
futures = "0.3"
anyhow = "1"
